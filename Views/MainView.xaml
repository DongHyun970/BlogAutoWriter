<Window x:Class="BlogAutoWriter.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Blog Generator" Height="600" Width="900"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- 🔎 입력 영역 -->
        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="블로그 키워드" FontSize="16" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBox x:Name="KeywordBox" Height="35"/>

            <TextBlock Text="작성 스타일" FontSize="16" Foreground="White" FontWeight="Bold" Margin="0,20,0,10"/>
            <ComboBox x:Name="StyleSelector" Height="35" SelectedIndex="0">
                <ComboBoxItem Content="일반 블로그"/>
                <ComboBoxItem Content="리뷰"/>
                <ComboBoxItem Content="에세이"/>
            </ComboBox>

            <Button x:Name="GenerateButton" Content="작성 요청" Height="40" Click="GenerateButton_Click" Margin="0,30,0,0"/>
        </StackPanel>

        <!-- 📄 미리보기 영역 -->
        <Border Grid.Column="1" Background="#2D2D40" Padding="15">
            <ScrollViewer>
                <TextBlock x:Name="PreviewTextBlock"
                           Text="여기에 작성된 블로그 글이 출력됩니다."
                           Foreground="White" TextWrapping="Wrap" FontSize="14"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>