<Window x:Class="BlogAutoWriter.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlogAutoWriter.Views"
        mc:Ignorable="d"
        Title="Login" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E2F">

    <Window.Resources>
        <Storyboard x:Key="ShakeAnimation" RepeatBehavior="1x">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="0" To="10" Duration="0:0:0.05" AutoReverse="True" RepeatBehavior="5x"/>
        </Storyboard>
    </Window.Resources>

    <Grid Margin="20" RenderTransformOrigin="0.5,0.5">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="WindowShaker" />
        </Grid.RenderTransform>

        <Border CornerRadius="15" Background="#2D2D40" Padding="20">
            <StackPanel>
                <TextBlock Text="Login to BlogAutoWriter" 
                           FontSize="24" 
                           Foreground="White" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- User ID -->
                <TextBox x:Name="UserIdTextBox" 
                         Height="40" 
                         Margin="0,0,0,15"
                         FontSize="16"
                         Foreground="White"
                         Background="#3E3E55"
                         local:PlaceholderBehavior.Placeholder="User ID"
                         BorderThickness="0"
                         Padding="10"/>

                <!-- Password -->
                <PasswordBox x:Name="PasswordBox" 
                             Height="40" 
                             Margin="0,0,0,15"
                             FontSize="16"
                             Foreground="White"
                             Background="#3E3E55"
                             local:PlaceholderBehavior.Placeholder="Password"
                             BorderThickness="0"
                             Padding="10"/>

                <!-- Login Button -->
                <Button Content="Login" 
                        Height="40" 
                        Background="#FF6F61"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderBrush="Transparent"
                        Cursor="Hand"
                        Click="LoginButton_Click"
                        Margin="0,10,0,0"
                        Padding="10"/>

                <!-- ProgressBar -->
                <ProgressBar x:Name="LoginProgressBar" Height="10" Visibility="Collapsed"
                             IsIndeterminate="True" Margin="0,15,0,0"/>

                <!-- Status Message -->
                <TextBlock x:Name="StatusText" 
                           Foreground="Orange" 
                           FontSize="14" 
                           Margin="0,15,0,0"
                           TextAlignment="Center"
                           Opacity="0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
